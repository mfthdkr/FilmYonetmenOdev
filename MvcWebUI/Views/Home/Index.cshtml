@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="text-left">
    <h1 class="display-4">Film Yönetmen Bilgi Sayfası</h1>
    <p>Hoşgeldiniz.</p>
    @if(TempData["Mesaj"] != null)
    {
        <p class="bg-success text-white">
            @TempData["Mesaj"]
        </p>
    }
</div>